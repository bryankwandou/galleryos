import Link from "next/link";
import { Check } from "lucide-react";
import { Logo } from "@/components/logo";

const tiers = [{name:"Solo",price:"$24",copy:"For photographers delivering a few sessions each month.",features:["5 active galleries","100 GB delivery storage","AI-assisted cull review","Studio branding"]},{name:"Studio",price:"$59",copy:"For busy independent studios with a full pipeline.",features:["20 active galleries","1 TB delivery storage","Booking and payment tracking","Priority processing"]},{name:"Collective",price:"$129",copy:"For small teams sharing clients and production work.",features:["Unlimited active galleries","3 TB delivery storage","Up to 8 studio members","Role-based access"]}];
export default function PricingPage(){return <main className="simple-page"><nav className="nav shell"><Logo/><Link className="button button-dark" href="/dashboard">Open studio</Link></nav><header className="simple-hero"><span className="kicker">Clear studio pricing</span><h1>Pay for the work<br/><em>you are delivering.</em></h1><p>Plans are based on active client galleries, not how many frames you shoot.</p></header><section className="pricing-grid shell">{tiers.map((tier,index)=><article className={index===1?"featured":""} key={tier.name}>{index===1&&<span className="popular">Most practical</span>}<h2>{tier.name}</h2><p>{tier.copy}</p><div className="price">{tier.price}<small>/month</small></div><Link href="/dashboard">Choose {tier.name}</Link><ul>{tier.features.map(item=><li key={item}><Check size={15}/>{item}</li>)}</ul></article>)}</section></main>}
